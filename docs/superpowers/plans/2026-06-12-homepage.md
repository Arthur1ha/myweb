# Personal Homepage Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a static personal homepage for eliottxu.top with Nav + Hero + Projects + Blog + About + Contact sections, styled per the design spec.

**Architecture:** Pure static HTML/CSS/JS single-page site. Three CSS files split by responsibility (global, layout, components). Two JS files for nav highlighting and entrance animations. No build tool — files are served as-is by Cloudflare Pages.

**Tech Stack:** HTML5, CSS3 (custom properties), vanilla JS (Intersection Observer API), Google Fonts CDN (Playfair Display + Inter)

---

## File Map

| File | Responsibility |
|------|---------------|
| `index.html` | All sections, semantic markup, Google Fonts link |
| `styles/main.css` | CSS variables, reset, base typography |
| `styles/layout.css` | Fixed nav, section spacing, grid/flex layout |
| `styles/components.css` | Cards, buttons, hover effects, animations |
| `js/nav.js` | Intersection Observer → active nav link highlight |
| `js/animations.js` | Fade-in-up entrance on scroll |
| `assets/projects/ssm-placeholder.png` | (manual step — add later) |
| `_redirects` | Cloudflare Pages routing (empty for now) |

---

## Task 1: Scaffold — file structure + Google Fonts

**Files:**
- Create: `index.html`
- Create: `styles/main.css`
- Create: `styles/layout.css`
- Create: `styles/components.css`
- Create: `js/nav.js`
- Create: `js/animations.js`
- Create: `_redirects`

- [ ] **Step 1: Create directory structure**

```bash
mkdir -p styles js assets/projects assets/fonts
touch styles/main.css styles/layout.css styles/components.css
touch js/nav.js js/animations.js
touch _redirects
```

- [ ] **Step 2: Create index.html with full skeleton**

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Eliott Xu</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,700;1,400&family=Inter:wght@400;500;600&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="styles/main.css" />
  <link rel="stylesheet" href="styles/layout.css" />
  <link rel="stylesheet" href="styles/components.css" />
</head>
<body>

  <!-- NAV -->
  <header class="nav">
    <div class="nav__inner">
      <a href="#hero" class="nav__logo">Eliott Xu</a>
      <button class="nav__hamburger" aria-label="Toggle menu" aria-expanded="false">
        <span></span><span></span><span></span>
      </button>
      <nav class="nav__links" id="navLinks">
        <a href="#about">About</a>
        <a href="#projects">Projects</a>
        <a href="#blog">Blog</a>
        <a href="#contact">Contact</a>
      </nav>
    </div>
  </header>

  <main>

    <!-- HERO -->
    <section id="hero" class="section section--hero">
      <div class="container">
        <p class="hero__eyebrow">Hi, I'm</p>
        <h1 class="hero__name">Eliott Xu</h1>
        <p class="hero__tagline">Tinkerer &amp; Creator</p>
        <p class="hero__bio">[Your intro here — a few sentences about who you are and what you do.]</p>
        <a href="#projects" class="btn">View Projects</a>
      </div>
    </section>

    <!-- PROJECTS -->
    <section id="projects" class="section">
      <div class="container">
        <h2 class="section__title">Projects</h2>
        <div class="projects-grid">
          <article class="project-card">
            <div class="project-card__image">
              <div class="project-card__placeholder">SSM</div>
            </div>
            <div class="project-card__body">
              <h3 class="project-card__name">SSM Application</h3>
              <p class="project-card__desc">[One-line description of your SSM project.]</p>
              <div class="project-card__tags">
                <span class="tag">Spring Boot</span>
                <span class="tag">MyBatis</span>
                <span class="tag">MySQL</span>
              </div>
              <a href="https://ssm.eliottxu.top" target="_blank" rel="noopener" class="project-card__link">View Project →</a>
            </div>
          </article>
        </div>
      </div>
    </section>

    <!-- BLOG -->
    <section id="blog" class="section section--alt">
      <div class="container">
        <h2 class="section__title">Blog</h2>
        <p class="coming-soon">Coming soon — thoughts and notes on building things.</p>
      </div>
    </section>

    <!-- ABOUT -->
    <section id="about" class="section">
      <div class="container">
        <h2 class="section__title">About</h2>
        <div class="about-content">
          <p>[Your background, technical direction, and interests go here.]</p>
        </div>
      </div>
    </section>

    <!-- CONTACT -->
    <section id="contact" class="section section--alt">
      <div class="container">
        <h2 class="section__title">Contact</h2>
        <div class="contact-links">
          <a href="https://github.com/Arthur1ha" target="_blank" rel="noopener" class="contact-link">
            <span class="contact-link__label">GitHub</span>
            <span class="contact-link__value">Arthur1ha</span>
          </a>
          <a href="mailto:[your@email.com]" class="contact-link">
            <span class="contact-link__label">Email</span>
            <span class="contact-link__value">[your@email.com]</span>
          </a>
        </div>
      </div>
    </section>

  </main>

  <footer class="footer">
    <div class="container">
      <p>© 2026 Eliott Xu</p>
    </div>
  </footer>

  <script src="js/nav.js"></script>
  <script src="js/animations.js"></script>
</body>
</html>
```

- [ ] **Step 3: Verify file opens in browser without errors**

Open `index.html` directly in a browser. Expected: unstyled but structurally correct HTML, no console errors.

- [ ] **Step 4: Commit**

```bash
git add index.html styles/ js/ _redirects
git commit -m "feat: scaffold homepage file structure"
```

---

## Task 2: Global styles — variables, reset, typography (main.css)

**Files:**
- Modify: `styles/main.css`

- [ ] **Step 1: Write main.css**

```css
/* ── Variables ── */
:root {
  --color-bg: #FAFAF8;
  --color-text: #1A1A1A;
  --color-accent: #C8A882;
  --color-secondary: #888880;
  --color-divider: #EBEBE8;

  --font-heading: 'Playfair Display', Georgia, serif;
  --font-body: 'Inter', system-ui, sans-serif;

  --nav-height: 64px;
  --max-width: 900px;
  --section-padding: 96px;
}

/* ── Reset ── */
*, *::before, *::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html {
  scroll-behavior: smooth;
}

body {
  background: var(--color-bg);
  color: var(--color-text);
  font-family: var(--font-body);
  font-size: 16px;
  line-height: 1.7;
  -webkit-font-smoothing: antialiased;
}

img {
  max-width: 100%;
  display: block;
}

a {
  color: inherit;
  text-decoration: none;
}

/* ── Base typography ── */
h1, h2, h3 {
  font-family: var(--font-heading);
  line-height: 1.15;
  color: var(--color-text);
}

/* ── Utilities ── */
.container {
  max-width: var(--max-width);
  margin: 0 auto;
  padding: 0 24px;
}

.section {
  padding: var(--section-padding) 0;
}

.section--alt {
  background: #F3F3F0;
}

.section__title {
  font-size: clamp(28px, 4vw, 40px);
  margin-bottom: 48px;
}
```

- [ ] **Step 2: Verify in browser**

Reload `index.html`. Expected: clean white-ish background, Inter font on body text, section padding visible.

- [ ] **Step 3: Commit**

```bash
git add styles/main.css
git commit -m "feat: add CSS variables, reset, and base typography"
```

---

## Task 3: Layout — fixed nav and section structure (layout.css)

**Files:**
- Modify: `styles/layout.css`

- [ ] **Step 1: Write layout.css**

```css
/* ── Fixed Navigation ── */
.nav {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  height: var(--nav-height);
  background: rgba(250, 250, 248, 0.92);
  backdrop-filter: blur(8px);
  border-bottom: 1px solid var(--color-divider);
  z-index: 100;
}

.nav__inner {
  max-width: var(--max-width);
  margin: 0 auto;
  padding: 0 24px;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.nav__logo {
  font-family: var(--font-heading);
  font-size: 18px;
  font-weight: 700;
  color: var(--color-text);
}

.nav__links {
  display: flex;
  gap: 32px;
}

.nav__links a {
  font-size: 14px;
  font-weight: 500;
  color: var(--color-secondary);
  position: relative;
  padding-bottom: 2px;
  transition: color 0.2s;
}

.nav__links a::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 0;
  width: 0;
  height: 1px;
  background: var(--color-accent);
  transition: width 0.25s ease;
}

.nav__links a:hover,
.nav__links a.active {
  color: var(--color-text);
}

.nav__links a:hover::after,
.nav__links a.active::after {
  width: 100%;
}

/* ── Hamburger (hidden on desktop) ── */
.nav__hamburger {
  display: none;
  flex-direction: column;
  gap: 5px;
  background: none;
  border: none;
  cursor: pointer;
  padding: 4px;
}

.nav__hamburger span {
  display: block;
  width: 22px;
  height: 2px;
  background: var(--color-text);
  transition: transform 0.2s, opacity 0.2s;
}

/* ── Hero section offset for fixed nav ── */
.section--hero {
  padding-top: calc(var(--nav-height) + var(--section-padding));
  min-height: 100vh;
  display: flex;
  align-items: center;
}

/* ── Footer ── */
.footer {
  padding: 32px 0;
  border-top: 1px solid var(--color-divider);
  text-align: center;
  color: var(--color-secondary);
  font-size: 13px;
}

/* ── Mobile ── */
@media (max-width: 640px) {
  .nav__hamburger {
    display: flex;
  }

  .nav__links {
    display: none;
    position: absolute;
    top: var(--nav-height);
    left: 0;
    right: 0;
    flex-direction: column;
    gap: 0;
    background: var(--color-bg);
    border-bottom: 1px solid var(--color-divider);
    padding: 16px 24px;
  }

  .nav__links.is-open {
    display: flex;
  }

  .nav__links a {
    padding: 12px 0;
    border-bottom: 1px solid var(--color-divider);
    font-size: 16px;
  }

  .nav__links a:last-child {
    border-bottom: none;
  }
}
```

- [ ] **Step 2: Verify in browser**

Reload. Expected: nav fixed at top with blur, sections spaced, Hero fills viewport height.

- [ ] **Step 3: Commit**

```bash
git add styles/layout.css
git commit -m "feat: add fixed nav layout and mobile hamburger structure"
```

---

## Task 4: Components — Hero, Cards, Buttons, Contact (components.css)

**Files:**
- Modify: `styles/components.css`

- [ ] **Step 1: Write components.css**

```css
/* ── Hero ── */
.hero__eyebrow {
  font-size: 14px;
  letter-spacing: 3px;
  text-transform: uppercase;
  color: var(--color-secondary);
  margin-bottom: 16px;
}

.hero__name {
  font-size: clamp(48px, 8vw, 88px);
  font-weight: 700;
  line-height: 1.0;
  margin-bottom: 16px;
}

.hero__tagline {
  font-family: var(--font-heading);
  font-size: clamp(20px, 3vw, 28px);
  font-style: italic;
  color: var(--color-accent);
  margin-bottom: 24px;
}

.hero__bio {
  max-width: 540px;
  color: var(--color-secondary);
  font-size: 16px;
  margin-bottom: 40px;
  line-height: 1.8;
}

/* ── Button ── */
.btn {
  display: inline-block;
  padding: 12px 28px;
  border: 1.5px solid var(--color-text);
  font-family: var(--font-body);
  font-size: 14px;
  font-weight: 500;
  letter-spacing: 0.5px;
  color: var(--color-text);
  transition: background 0.2s, color 0.2s;
}

.btn:hover {
  background: var(--color-text);
  color: var(--color-bg);
}

/* ── Projects grid ── */
.projects-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
  gap: 32px;
}

.project-card {
  border: 1px solid var(--color-divider);
  overflow: hidden;
  transition: transform 0.25s ease, box-shadow 0.25s ease;
}

.project-card:hover {
  transform: translateY(-4px);
  box-shadow: 0 12px 32px rgba(0, 0, 0, 0.08);
}

.project-card__image {
  aspect-ratio: 16 / 9;
  overflow: hidden;
  background: var(--color-divider);
}

.project-card__image img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.project-card__placeholder {
  width: 100%;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-family: var(--font-heading);
  font-size: 32px;
  color: var(--color-secondary);
  background: #EBEBЕ8;
}

.project-card__body {
  padding: 24px;
}

.project-card__name {
  font-size: 20px;
  margin-bottom: 8px;
}

.project-card__desc {
  font-size: 14px;
  color: var(--color-secondary);
  margin-bottom: 16px;
  line-height: 1.6;
}

.project-card__tags {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  margin-bottom: 20px;
}

.tag {
  font-size: 11px;
  letter-spacing: 0.5px;
  padding: 3px 10px;
  border: 1px solid var(--color-divider);
  color: var(--color-secondary);
}

.project-card__link {
  font-size: 14px;
  font-weight: 500;
  color: var(--color-accent);
  transition: letter-spacing 0.2s;
}

.project-card__link:hover {
  letter-spacing: 0.5px;
}

/* ── Blog ── */
.coming-soon {
  color: var(--color-secondary);
  font-style: italic;
}

/* ── About ── */
.about-content {
  max-width: 620px;
}

.about-content p {
  font-size: 16px;
  color: var(--color-secondary);
  line-height: 1.85;
}

/* ── Contact ── */
.contact-links {
  display: flex;
  flex-direction: column;
  gap: 0;
  max-width: 400px;
}

.contact-link {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 20px 0;
  border-bottom: 1px solid var(--color-divider);
  transition: color 0.2s;
}

.contact-link:first-child {
  border-top: 1px solid var(--color-divider);
}

.contact-link:hover {
  color: var(--color-accent);
}

.contact-link__label {
  font-size: 12px;
  letter-spacing: 2px;
  text-transform: uppercase;
  color: var(--color-secondary);
}

.contact-link__value {
  font-size: 14px;
  font-weight: 500;
}
```

- [ ] **Step 2: Verify in browser**

Reload. Expected: Hero has large name, italic tagline in accent color; Project card visible with placeholder; Contact rows styled; button visible.

- [ ] **Step 3: Commit**

```bash
git add styles/components.css
git commit -m "feat: add Hero, card, button, and contact component styles"
```

---

## Task 5: nav.js — Intersection Observer active link highlighting + hamburger

**Files:**
- Modify: `js/nav.js`

- [ ] **Step 1: Write nav.js**

```js
(function () {
  const sections = document.querySelectorAll('main section[id]');
  const navLinks = document.querySelectorAll('.nav__links a');

  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          navLinks.forEach((link) => link.classList.remove('active'));
          const active = document.querySelector(`.nav__links a[href="#${entry.target.id}"]`);
          if (active) active.classList.add('active');
        }
      });
    },
    { rootMargin: '-40% 0px -55% 0px' }
  );

  sections.forEach((s) => observer.observe(s));

  // Hamburger toggle
  const hamburger = document.querySelector('.nav__hamburger');
  const navLinksEl = document.getElementById('navLinks');

  hamburger.addEventListener('click', () => {
    const isOpen = navLinksEl.classList.toggle('is-open');
    hamburger.setAttribute('aria-expanded', isOpen);
  });

  // Close menu on link click (mobile)
  navLinksEl.querySelectorAll('a').forEach((link) => {
    link.addEventListener('click', () => {
      navLinksEl.classList.remove('is-open');
      hamburger.setAttribute('aria-expanded', false);
    });
  });
})();
```

- [ ] **Step 2: Verify in browser**

Scroll down through sections. Expected: nav link for the visible section gets underline highlight. On mobile viewport, hamburger opens/closes the menu.

- [ ] **Step 3: Commit**

```bash
git add js/nav.js
git commit -m "feat: add Intersection Observer nav highlighting and hamburger toggle"
```

---

## Task 6: animations.js — fade-in-up entrance on scroll

**Files:**
- Modify: `js/animations.js`
- Modify: `styles/components.css` (add animation base class)

- [ ] **Step 1: Add animation base CSS to components.css**

Append to `styles/components.css`:

```css
/* ── Entrance animations ── */
.fade-up {
  opacity: 0;
  transform: translateY(20px);
  transition: opacity 0.6s ease, transform 0.6s ease;
}

.fade-up.is-visible {
  opacity: 1;
  transform: translateY(0);
}
```

- [ ] **Step 2: Add `fade-up` class to animated elements in index.html**

In `index.html`, add `class="fade-up"` to the following elements:

- `.hero__eyebrow` → `<p class="hero__eyebrow fade-up">`
- `.hero__name` → `<h1 class="hero__name fade-up">`
- `.hero__tagline` → `<p class="hero__tagline fade-up">`
- `.hero__bio` → `<p class="hero__bio fade-up">`
- `.btn` in hero → `<a href="#projects" class="btn fade-up">`
- `.project-card` → `<article class="project-card fade-up">`
- `.section__title` in each section → add `fade-up` to each
- `.coming-soon` → `<p class="coming-soon fade-up">`
- `.about-content` → `<div class="about-content fade-up">`
- `.contact-links` → `<div class="contact-links fade-up">`

- [ ] **Step 3: Write animations.js**

```js
(function () {
  const targets = document.querySelectorAll('.fade-up');

  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.add('is-visible');
          observer.unobserve(entry.target);
        }
      });
    },
    { threshold: 0.15 }
  );

  targets.forEach((el) => observer.observe(el));
})();
```

- [ ] **Step 4: Add staggered delay for Hero elements**

Append to `styles/components.css`:

```css
.hero__eyebrow.fade-up  { transition-delay: 0s; }
.hero__name.fade-up     { transition-delay: 0.1s; }
.hero__tagline.fade-up  { transition-delay: 0.2s; }
.hero__bio.fade-up      { transition-delay: 0.3s; }
.hero .btn.fade-up      { transition-delay: 0.4s; }
```

- [ ] **Step 5: Verify in browser**

Hard-refresh the page. Expected: Hero elements fade in one by one with stagger. Scrolling down triggers fade-in for sections.

- [ ] **Step 6: Commit**

```bash
git add js/animations.js styles/components.css index.html
git commit -m "feat: add fade-in-up scroll entrance animations with hero stagger"
```

---

## Task 7: Final polish — mobile layout, footer, gitignore

**Files:**
- Modify: `styles/layout.css` (verify mobile sections)
- Create: `.gitignore`

- [ ] **Step 1: Add mobile section padding to layout.css**

Append to `styles/layout.css`:

```css
@media (max-width: 640px) {
  :root {
    --section-padding: 64px;
  }

  .projects-grid {
    grid-template-columns: 1fr;
  }
}
```

- [ ] **Step 2: Create .gitignore**

```
.superpowers/
.DS_Store
Thumbs.db
```

- [ ] **Step 3: Verify full page on mobile viewport**

In browser DevTools, switch to a 390px wide viewport (iPhone). Expected: hamburger menu visible, single-column project cards, nav links hidden until hamburger clicked, all sections readable.

- [ ] **Step 4: Final desktop check**

Switch back to desktop viewport. Scroll through all sections. Expected:
- Nav stays fixed with blur
- Hero fills screen, name large, tagline in accent color
- Project card hover lifts with shadow
- Blog shows "Coming soon" italic
- About placeholder visible
- Contact rows with hover color
- Footer at bottom

- [ ] **Step 5: Commit**

```bash
git add .gitignore styles/layout.css
git commit -m "feat: mobile polish and gitignore"
```

---

## Self-Review Checklist

- [x] **Spec coverage:** Fixed nav ✓, Hero (name + tagline + bio placeholder + CTA) ✓, Projects (SSM card + link) ✓, Blog (coming soon) ✓, About (placeholder) ✓, Contact (GitHub + email) ✓, Fonts (Playfair + Inter) ✓, Colors (all 5 defined as CSS vars) ✓, Animations (fade-up, nav hover underline, card hover) ✓, Mobile hamburger ✓, `_redirects` ✓
- [x] **No placeholders** in implementation code — user content areas are clearly marked with `[brackets]` in HTML
- [x] **Type consistency** — `.fade-up` / `.is-visible` consistent across CSS and JS; `.nav__links` / `#navLinks` consistent across HTML, layout.css, nav.js
